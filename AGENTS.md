# Project Instructions

This repo is no longer an active `career-ops` checkout. The original project is archived at `inspo/career-ops/` for reference.

## Current Objective

Help design and build Marcus's own simpler job application helper from the clean repo root.

## Reference Policy

- Treat `inspo/career-ops/` as read-only inspiration unless the user explicitly asks to modify or extract from it.
- Do not run `inspo/career-ops/update-system.mjs apply` or other upstream update flows unless the user specifically asks to maintain the archived copy.
- Do not apply the archived `career-ops` onboarding/update rules to the root project.
- Preserve personal artifacts in the archive unless the user asks to migrate or remove them.

## Development Style

- Keep root-level additions small and intentional while the new roadmap is being defined.
- Prefer simple markdown/data-first workflows until the desired helper shape is clear.
- Borrow implementation from the archive only when it aligns with the new helper's roadmap.
